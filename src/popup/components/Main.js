import React, { PropTypes } from 'react';

export default function Main(props) {
  return (
    <div>
      <h1>kaeru</h1>
      <div>
        <button>make card</button>
      </div>
    </div>
  );
}

Main.propTypes = {
};
